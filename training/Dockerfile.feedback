FROM python:3.9-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    boto3 \
    pandas \
    requests

COPY feedback_capture.py .

CMD ["python", "feedback_capture.py", "--continuous", "--interval", "3600"]
